.env
node_modules/
__pycache__/
*.pyc
*.pyo
dist/
.vite/
plantilla-frontend/

# Fotos JPG capturadas por el ESP32 (se guardan en disco, no en git)
backend/capturas/
capturas/
*.jpg
fotos_test/

# Datos de entrenamiento y capturas (NO se versionan: son pesados)
data/
*.zip
# Entornos virtuales de Python
.venv/
venv/
# Artefactos de entrenamiento (los .pt/.onnx de salida). Los modelos SERVIDOS
# viven en models/ y SÍ se versionan (no se ignoran aquí).
ml_antracnosis/outputs/